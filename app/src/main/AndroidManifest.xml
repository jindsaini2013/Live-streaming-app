<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network and streaming permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Audio permissions for VLC streaming -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Storage permissions for recordings and cache -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- Camera permission (for future features) -->
    <uses-permission android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />

    <!-- Notification permissions for Android 13+ -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".LiveStreamingApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Educational_App"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <!-- Login Activity (Main Entry Point) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.Educational_App.NoActionBar"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Student Main Activity -->
        <activity
            android:name=".StudentMainActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App.NoActionBar"
            android:screenOrientation="portrait"
            android:launchMode="singleTop" />

        <!-- Teacher Main Activity -->
        <activity
            android:name=".TeacherMainActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App.NoActionBar"
            android:screenOrientation="portrait"
            android:launchMode="singleTop" />

        <!-- Live Streaming Activity (Your existing MainActivity) -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App.Fullscreen"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:keepScreenOn="true"
            android:parentActivityName=".TeacherMainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TeacherMainActivity" />
        </activity>

        <!-- Attendance Activity for Teachers -->
        <activity
            android:name=".AttendanceActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App"
            android:parentActivityName=".TeacherMainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TeacherMainActivity" />
        </activity>

        <!-- Quiz Activity for Students -->
        <activity
            android:name=".QuizActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App"
            android:parentActivityName=".StudentMainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StudentMainActivity" />
        </activity>

        <!-- Video Player Activity for Recordings -->
        <activity
            android:name=".VideoPlayerActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App.Fullscreen"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:keepScreenOn="true"
            android:parentActivityName=".StudentMainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StudentMainActivity" />
        </activity>

        <!-- Chat Activity for Students -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App"
            android:parentActivityName=".StudentMainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".StudentMainActivity" />
        </activity>


        <activity
            android:name=".TeacherQuizUploadActivity"
            android:exported="false"
            android:theme="@style/Theme.Educational_App"
            android:parentActivityName=".TeacherMainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TeacherMainActivity" />
        </activity>

        <activity android:name=".Register"
            android:parentActivityName=".TeacherMainActivity"></activity>

        <!-- File Provider for sharing files and images -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>



    </application>

</manifest>
